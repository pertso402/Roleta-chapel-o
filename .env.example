# ─── SUPABASE ────────────────────────────────────────────────────────────────
# Mesmo projeto do ERP / agente de atendimento / agente de recompra.
SUPABASE_URL=https://qlswjefuinhbtlhauhgj.supabase.co

# NUNCA prefixe com NEXT_PUBLIC_. A service key só existe nas rotas /api.
# Se ela vazar pro bundle do browser, qualquer pessoa emite cupom de graça.
SUPABASE_SERVICE_KEY=

# ─── WHATSAPP ────────────────────────────────────────────────────────────────
# Numero do WhatsApp que recebe o resgate, so digitos com DDI.
# +55 44 3771-1475 (fixo da loja). wa.me so funciona se este numero estiver
# registrado no WhatsApp Business — testar o link antes de imprimir adesivo.
WHATSAPP_NUMERO=554437711475

# ─── PAINEL ──────────────────────────────────────────────────────────────────
# Senha unica do painel /painel. Trocar antes de subir.
PAINEL_SENHA=

# Sal do hash de IP usado no rate limit. Qualquer string longa e aleatoria.
# Trocar o sal zera o rate limit de todo mundo, entao nao troque em producao.
IP_SALT=
